global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "johnrahmadi-model-serving"
    metrics_path: /monitoring/prometheus/metrics
    scheme: https
    static_configs:
      - targets: ["submission-mlops-johnrahmadi-production.up.railway.app"]
